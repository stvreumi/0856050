language: java
dist: bionic
jdk:
  - openjdk11

install:
  - sudo apt-get update
  - sudo apt-get -y install ant
  - echo $(ant -version)

before_script:
  - echo `ant --version`

script:
  - ant test